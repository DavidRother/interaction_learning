from setuptools import setup, find_packages

setup(name='interaction_learning',
      version='0.0.1',
      description='Learning Interactions',
      author='David Rother',
      email='david@edv-drucksysteme.de',
      packages=find_packages() + [""],
      install_requires=[
      ]
      )
